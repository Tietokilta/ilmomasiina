[package]
name = "ilmomasiina_receiptgen"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi8"] }
napi-derive = "2"

laskugeneraattori = { path = "../vendor/laskugeneraattori" }
garde = "0.22.1"
typst-pdf = "0.13.1"
serde_json = "1.0.149"

[build-dependencies]
napi-build = "2"
