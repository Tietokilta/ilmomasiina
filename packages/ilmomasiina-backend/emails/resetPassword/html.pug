extends ../layout.pug

block mainContent
	div.content-block
		p.bodyText Your password to Ilmomasiina has been reset.
		ul
			each val in fields
				li
					strong #{val.label}:
					| #{val.answer}
	p.bodyText You can log in at #[a(href=siteUrl) #{siteUrl}].
